<h1>First Page Auto Shop</h1>

<% if current_user.nil? %>
  <h2>Please sign in!</h2>
  <h2>
  <%= link_to "Sign up now!", new_user_registration_path, class: "btn btn-large" %>
  <%= link_to "Sign in now!", new_user_session_path, class: "btn btn-large" %>
  </h2>
<% end %>
