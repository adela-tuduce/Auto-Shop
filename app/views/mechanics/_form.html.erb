<%= form_for(@mechanic) do |f| %>
  <% if @mechanic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mechanic.errors.count, "error") %> prohibited this mechanic from being saved:</h2>

      <ul>
      <% @mechanic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :age %><br>
    <%= f.number_field :age %>
  </div>
  <div class="field">
    <%= f.label :experience %><br>
    <%= f.number_field :experience %>
  </div>
  <div class="field">
    <%= f.label :name, "Services" %><br>
    <%= select("Services", "name", options_for_select(Service.all.map{ |p| [p.name, p.id] }), {:prompt => 'Select a service...'}) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
